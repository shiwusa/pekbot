const DB_USER = `shiwusa`;
const DB_PASSWORD = `shiwusa123`;
export const DB_URI = `mongodb+srv://${DB_USER}:${DB_PASSWORD}@cluster0.9xzd3.mongodb.net/pekbot?retryWrites=true&w=majority`;

export const TOKEN = ``;